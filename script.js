// form handling and working with forms
// handle backend process of forms and making sure the data coming from any frontend library,fw,templating engines,we still handle it at backend

// cookie and session 
//hum log khch bhi data frontend par browser per rkh skte hai and jab bhi aap khch bhi request backend per karoge wo frontend per saved data automatically backend per chla jaega